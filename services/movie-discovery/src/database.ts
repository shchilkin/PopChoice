export {
  initDatabase,
  closeDatabase,
  filterNewMovies,
  ensureSchema,
  getMovieCount,
  insertMovies,
  upsertMovieCatalogMetadata,
} from '@pop-choice/shared';
export type { MovieRecord } from '@pop-choice/shared';
