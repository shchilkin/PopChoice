apiVersion: 1

datasources:
  - name: Tempo
    type: tempo
    access: proxy
    url: http://observability-tempo:3200
    uid: tempo
    editable: true
    jsonData:
      tracesToLogsV2:
        datasourceUid: loki
        filterByTraceID: true
        spanStartTimeShift: -5m
        spanEndTimeShift: 5m
